# Спецификация: Полная актуализация сделок из WhatsApp-чата и синхронизация с Bitrix24 CRM

**Дата и время создания:** 2026-09-25 18:03:19 MSK  
**Тип задачи:** feature  
**Ветка:** `feature/crm-chat-actualization`  
**Ответственный компонент:** `backend/api/` (management command, BitrixService, tasks, RawMessage, Project, Commitment, FinancialRecord, BitrixDealChangeLog)

---

## 1. Архитектура и диаграммы

### 1.1. ERD задействованных сущностей базы данных (Mermaid)

```mermaid
erDiagram
    Project ||--o{ Commitment : "имеет обязательства"
    Project ||--o{ FinancialRecord : "имеет оплаты"
    Project ||--o{ BitrixDealChangeLog : "логирует изменения CRM"
    RawMessage ||--o{ Commitment : "источник обязательства"
    UserProfile ||--o{ Project : "ответственный менеджер"
    UserProfile ||--o{ Commitment : "исполнитель"
    Company ||--o{ Project : "заказчик"

    Project {
        bigint id PK
        string name "Название сделки/объекта"
        string normalized_name "Нормализованное имя для дедупликации"
        string bitrix_id "ID сделки в Bitrix24 (#1234)"
        string status "Стадия сделки"
        decimal contract_amount "Сумма сделки"
        decimal cost_amount "Себестоимость"
        decimal paid_amount "Оплачено"
        string decision_maker "ЛПР"
        string blocker "Ключевой блокер"
        string current_action "Текущее действие"
        string next_action "Следующий шаг"
        boolean needs_bitrix_sync "Флаг синхронизации"
    }

    RawMessage {
        bigint id PK
        string message_id "Уникальный ID сообщения WAHA"
        string sender_name "Имя отправителя"
        string sender_phone "Телефон отправителя"
        datetime timestamp "Время отправки"
        text content "Текст сообщения"
        boolean processed "Обработано"
    }

    Commitment {
        bigint id PK
        bigint project_id FK
        bigint manager_id FK
        bigint source_message_id FK
        text commitment_text "Обещание / следующий шаг"
        date deadline "Срок выполнения"
        string status "pending / in_progress / completed"
        string severity "critical / high / medium / low"
        string bitrix_task_id "ID связанной задачи в Bitrix24"
    }

    FinancialRecord {
        bigint id PK
        bigint project_id FK
        decimal amount "Сумма транзакции"
        date payment_date "Дата оплаты"
        string payment_type "advance / milestone / final"
        string status "received"
    }

    BitrixDealChangeLog {
        bigint id PK
        bigint project_id FK
        string bitrix_deal_id "ID сделки Bitrix24"
        string action "create / update / comment / task"
        string triggered_by "chat_actualization / webhook / system"
        jsonb changed_fields "Diff изменений"
        jsonb snapshot_before "Снимок до"
        jsonb snapshot_after "Снимок после"
        boolean success "Успешно отправлено"
        datetime created_at "Время логирования"
    }
```

### 1.2. Sequence-диаграмма процесса сквозной актуализации

```mermaid
sequenceDiagram
    autonumber
    actor Admin as Администратор / Cron
    participant Script as actualize_crm_from_chat (Command)
    participant B24 as Bitrix24 REST API (Cloud)
    participant DB as Mazory PostgreSQL (DB)
    participant Archive as Архив сообщений (732 шт.)
    participant WAHA as WAHA (Noweb live)
    participant Audit as BitrixDealChangeLog

    Admin->>Script: Запуск актуализации (--dry-run=False)
    
    rect rgb(240, 248, 255)
        note over Script, B24: Шаг 1. Актуализация каталога всех сделок Bitrix24
        Script->>B24: crm.deal.list (paged, 576 сделок)
        B24-->>Script: Список всех сделок (ID, TITLE, STAGE_ID, OPPORTUNITY, etc.)
        Script->>DB: Сохранение/обновление сделок Project (bitrix_id, normalized_name)
    end

    rect rgb(255, 250, 240)
        note over Script, Archive: Шаг 2. Загрузка сообщений и декомпозиция отчетов
        Script->>Archive: Чтение messages.json (732 сообщения)
        Script->>WAHA: Запрос последних сообщений чата
        WAHA-->>Script: Свежие сообщения
        Script->>DB: Идемпотентная запись в RawMessage (get_or_create)
        Script->>Script: Разбивка составных ежедневных отчетов на блоки по объектам
    end

    rect rgb(240, 255, 240)
        note over Script, DB: Шаг 3. Извлечение фактов и привязка к сделкам
        loop Для каждого фрагмента отчета
            Script->>Script: Извлечение сумм, стадий, замен оборудования, обязательств
            Script->>DB: Нечеткий поиск существующего Project по имени/нормализации
            alt Сделка найдена
                Script->>DB: Обновление Project (сумма, статус, текущий/след шаг, блокер)
            else Сделка новая (высокая уверенность)
                Script->>DB: Создание нового Project
            end
            Script->>DB: Создание Commitment (дедлайны, обещания)
            Script->>DB: Создание FinancialRecord (оплаты)
        end
    end

    rect rgb(255, 240, 245)
        note over Script, B24: Шаг 4. Пакетная синхронизация с Bitrix24 (с троттлингом 0.7 сек)
        loop Для каждой сделки с needs_bitrix_sync = True
            Script->>B24: crm.deal.update (ID, STAGE_ID, OPPORTUNITY, COMMENTS)
            B24-->>Script: Success
            Script->>Audit: Логирование в BitrixDealChangeLog (snapshot_before, snapshot_after)
            Script->>B24: crm.timeline.comment.add (хронология фактов из чата)
            B24-->>Script: Success
            opt Актуальное обязательство со сроком >= сегодня
                Script->>B24: tasks.task.add (дедлайн, ответственный)
                B24-->>Script: Task ID
                Script->>DB: Сохранение bitrix_task_id в Commitment
            end
            Script->>Script: sleep(0.7) (защита от лимита 2 req/s)
        end
    end

    Script-->>Admin: Отчет о завершении (обновлено сделок, создано логов, обязательств)
```

---

## 2. Постановка и цели

1. Выполнить полную и глубокую актуализацию данных между рабочим чатом WhatsApp «КОМАНДА ПОДДЕРЖКИ ПРОДАЖ» и CRM Bitrix24 на продакшене `https://ai.mazory.best/`.
2. Предотвратить потерю информации из многообъектных ежедневных отчетов (декомпозиция отчетов).
3. Исключить создание дубликатов в Bitrix24 за счет предварительного импорта каталога из 576 существующих сделок Bitrix24 и нормализованного сопоставления.
4. Предотвратить шторм уведомлений в Bitrix24: исторические обещания пишутся в таймлайн сделки, а задачи создаются только по активным дедлайнам.
5. Соблюсти лимит вызовов Bitrix24 REST API (<= 2 запроса в секунду).
6. Зафиксировать все до единого изменения в `BitrixDealChangeLog` с возможностью просмотра в Django Admin.

---

## 3. Требования

### Функциональные требования
1. **Management command `actualize_crm_from_chat`:**
   * Поддержка параметров `--dry-run`, `--limit-messages`, `--skip-bitrix-push`.
   * Шаг 1: `sync_bitrix_deal_catalog()` — загрузка всех 576 сделок из Bitrix24 в локальную БД `Project`.
   * Шаг 2: `load_raw_messages()` — загрузка 732 сообщений из `/app/archive_data/messages.json` + свежих сообщений из WAHA в `RawMessage`.
   * Шаг 3: `parse_and_extract_facts()` — извлечение коммерческих событий из отчетов с парсингом многообъектных отчетов.
   * Шаг 4: `sync_to_bitrix()` — синхронизация сделок с Bitrix24 с интервалом 0.7с между вызовами, фиксацией в `BitrixDealChangeLog`, добавлением комментариев в таймлайн сделки и созданием задач только для активных дедлайнов.
2. **Нечеткий матчинг и нормализация:**
   * Обработка вариаций написания: «Оазис» / «Оазиз» (`#1396`), «ЦТП 343» (`#718`), «Курмет» (`#350`), «Олимпик Парк» (`#304`), «Казына» (`#936`, `#938`), «Vertex Garden».
3. **Логирование изменений:**
   * Все вызовы `crm.deal.update` логируются через существующий `BitrixService._record_deal_log` в модель `BitrixDealChangeLog` со статусом `success=True`.
4. **Безопасность и отказоустойчивость:**
   * Идемпотентность — повторный запуск не создает дубликатов сделок, сообщений или записей оплат.

---

## 4. Критерии готовности (Definition of Done)

1. Команда `actualize_crm_from_chat` реализована, протестирована и запускается без ошибок.
2. В локальной базе данных актуализированы `Project`, `RawMessage`, `Commitment`, `FinancialRecord`.
3. Все обновления сделок успешно переданы в Bitrix24 CRM с соблюдением лимитов REST API.
4. В админке `https://ai.mazory.best/admin/api/bitrixdealchangelog/` доступны записи всех изменений.
5. Прогон системного check Django (`python manage.py check`) и миграций (`python manage.py makemigrations --check`) завершается с кодом 0.
